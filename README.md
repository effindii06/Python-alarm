# Python-alarm
A robust command-line automation tool engineered in Python that tracks real-time system clocks using the datetime module. It features robust user input validation to prevent crashes, calculates an active countdown time remaining status bar.
